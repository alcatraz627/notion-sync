{
  "name": "notion-sync",
  "version": "1.3.0",
  "description": "Syncs markdown docs to Notion — standalone tool, not part of any app",
  "main": "index.ts",
  "scripts": {
    "sync": "bun index.ts",
    "sync:dry": "DRY_RUN=1 bun index.ts"
  },
  "dependencies": {
    "@notionhq/client": "^5.20.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
